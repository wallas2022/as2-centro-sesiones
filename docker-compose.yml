version: '3.8'

services:
  keycloak:
    image: quay.io/keycloak/keycloak:24.0.3
    command: [
      "start", 
      "--http-enabled=true",
      "--hostname-strict=false",
      "--proxy=edge",
      "--db=postgres",
      "--db-url=jdbc:postgresql://tu-postgres-url",
      "--db-username=usuario",
      "--db-password=contraseña"
    ]
    ports:
      - "8080:8080"
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HOSTNAME: tu-dominio.render.com
    volumes:
      - keycloak_data:/opt/keycloak/data

volumes:
  keycloak_data: